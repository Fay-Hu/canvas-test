import { DropShapeGroupDirective } from './drop-shape-group.directive';

describe('DropShapeGroupDirective', () => {
  it('should create an instance', () => {
    const directive = new DropShapeGroupDirective();
    expect(directive).toBeTruthy();
  });
});
