import { DropShapeDirective } from './drop-shape.directive';

describe('DropShapeDirective', () => {
  it('should create an instance', () => {
    const directive = new DropShapeDirective();
    expect(directive).toBeTruthy();
  });
});
