import { HignlightDirective } from './hignlight.directive';

describe('HignlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HignlightDirective();
    expect(directive).toBeTruthy();
  });
});
